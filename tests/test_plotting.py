import numpy as np

from mf_kmc.benchmark.plot_sweep import compute_area_difference


def test_area_difference():
    """Dummy test for area difference computation."""
    pass
    # # simulates single fidelity regret
    # sf = np.array([10, 8, 6, 4])

    # # simulates multi-fidelity regret
    # mf = np.array([np.nan, 10, 6, 2])

    # # simulates budgets
    # x = np.array([1, 2, 3, 4])

    # area = compute_area_difference(sf, mf, x)

    # assert area == -1.0, f"Wrong area computation, should be -1.0 but returns {area}"
